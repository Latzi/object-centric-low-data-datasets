\# COCO PottedPlant Script Requirements



The reconstruction / preparation pipeline for the \*\*COCO PottedPlant\*\* subset requires a Python environment with the following external packages:



\- `opencv-python`

\- `tqdm`



Install them with:



```bash

pip install -r requirements\_coco.txt

